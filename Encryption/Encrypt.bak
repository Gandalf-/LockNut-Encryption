#lang racket

;FILE
;Moves the file into a string
(require racket/port)
;(require "caesar-cipher")

(define (file->string filename)
  (port->string (open-input-file filename)))

;Moves the file into a list
(define list-chars (string->list (string-downcase (file->string "ShortEnciphered.txt"))))